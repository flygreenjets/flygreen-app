import {View} from "react-native";

interface SegmentItemProps {
    // Define the props for SegmentItem here
}

export default function SegmentItem({}: SegmentItemProps) {
    return (
        <View>

        </View>
    );
}